# AdaptiveDynamics_CH_RS

The code for this project is in the folder "Julia_Version" and uses the Julia programming language. The folder project.toml file loads the required packages. There is also the package "CooperativeHunting" that is contained in this folder (not a published package) that is loaded and uses for many of the functions.
